/** @type {import('next').NextConfig} */
const nextConfig = {
    // devIndicators: false
    // output: 'export',

    images: {
        formats: ['image/avif', 'image/webp']
    }
};

export default nextConfig;
